[Unit]
Description=Run KTC Mail ACME renewal twice daily

[Timer]
# Once daily with randomized delay — sufficient for LE rate limits
OnCalendar=daily
RandomizedDelaySec=6h
Persistent=true
Unit=ktc-mail-acme-renew.service

[Install]
WantedBy=timers.target
